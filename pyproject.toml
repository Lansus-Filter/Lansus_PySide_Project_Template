[project]
name = "lansus-pyside-project-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyside6==6.6.3",
]

[dependency-groups]
dev = [
    "nuitka>=2.7.12",
]
